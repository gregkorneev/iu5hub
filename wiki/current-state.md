# Current state

- **Project status:** Telegram-only MVP published to Cloudflare Pages at `https://iu5hub.pages.dev`, configured in BotFather and successfully opened from Telegram. Pages is connected to `gregkorneev/iu5hub`: pushes to `main` start the production build automatically. CI verification and the disabled-by-default Cloud.ru/GitHub Actions release paths remain optional automation paths.
- **First-launch onboarding (2026-09-25):** a three-step modal introduces Catalog, Search, Schedule and Profile favorites on the first browser launch. Skip, «Начать», or Escape stores completion in localStorage; reloads open the app directly. The modal stays above the current HashRouter destination and pauses Telegram BackButton until dismissed. Browser QA covers keyboard focus, direct nested URLs, light/dark Telegram theme, 320×568 safe area and persistence. Actual Telegram WebView behavior remains device-only validation.
- **Current milestone:** personal Telegram profile and D1-backed favorites released alongside private analytics, protected admin dashboard and `/stats`. Production D1 migration `0002_favorites.sql`, separate Worker secret, Worker deployment and Pages build are complete; real Telegram account/device smoke checks remain. See `profile.md` and ADR-0004.
- **Schedule integration (2026-09-25):** `/schedule` is now the fourth bottom destination. A real LKS inspection found 70 IU5 groups; the first static sync generated 70 JSON files, with 42 calendars and 28 groups whose public schedule is not yet posted. LKS-31Б ICS is public (HTTP 200, valid VCALENDAR) but incorrectly labeled `text/html`; its 25 events include 11 weekly and 14 biweekly series. `INTERVAL=2` and LKS `ch`/`zn` metadata establish the semester cycle anchor `2026-08-31`. Recurrence is expanded at sync time in Moscow time; no runtime LKS requests. D1 stores only the user's selected group slug, pending migration `0003` and Worker release. See `schedule.md`.
- **Schedule production status (2026-09-25):** D1 migration `0003_profile_preferences.sql` is applied and Worker deployed. Full `npm run qa` passed (105/105 Playwright, 24 Vitest, 54 Node tests, static validation and production build). Commit `7e2517f` is pushed to `main`; Cloudflare Pages deployed it, and production `/data/schedule/groups.json` and `/data/schedule/groups/iu5-31b.json` both return JSON successfully. Physical Telegram WebView and real cross-device checks remain device-dependent.
- **Mobile viewport scrolling (2026-09-25):** Home, Schedule and Profile use the Telegram viewport without document scrolling; Home/Profile fit at 390×844. After the screenshot report showed an internal Home scrollbar, Home spacing/cards/footer were compacted for short portrait viewports, and the Telegram top safe-area header no longer overlaps the greeting. Dark-theme Chromium/WebKit QA with 24px Telegram safe insets, short and long names, checked 295×667, 320×568, 360×667, 390×720 and 390×730: the document and `main` have no scroll range, all courses, Useful Links, footer and tabs remain visible, and there is no overlap/clipping. Schedule Today plus Week/day selection show two complete lessons at 390×844 and 320×568; only additional lessons scroll inside the named, keyboard-focusable region. In landscape, day controls stay visible and only lessons scroll. Physical Telegram WebView remains device-dependent and unverified.
- **Final layout QA correction (2026-09-27):** Telegram top safe-area was being applied both as `.app` top padding and as the sticky header offset; this shifted the header visually 24px while main retained its unshifted flow position, creating overlap on Profile and Schedule. Header now sticks at the top of the already inset app content; its safe-area surface fills the inset. Regression coverage verifies Profile/Schedule at 320×568 and 390×844 with a 24px inset in Chromium/WebKit; two-lesson portrait fit and landscape lesson-only scrolling remain passing. Full visual matrix covered 13 student routes plus authorized admin Statistics across 20 portrait/landscape/mobile/tablet/desktop viewports in Chromium/WebKit (560 route renders); lint, typecheck, 24 Vitest, 54 Node, static validations, production build, and 138 Playwright tests pass. Physical Telegram WebView/device keyboard remains unverified.
- **Home vertical rhythm (2026-09-26):** token-based mobile spacing now separates greeting, hero eyebrow/title/description, courses section, and course cards more clearly. A compact short-height override preserves the no-scroll home layout on 320×568; Playwright checks the vertical gaps and viewport fit through 430×932.
- **Home Favorites block (2026-09-27):** a count-and-link card sits between courses and Useful Links only after favorites load successfully and at least one item exists, reading the existing `FavoritesContext` and opening the existing Profile page. Normal mobile portrait viewports use 24px section gaps around Favorites; short screens keep the compact no-scroll layout. Playwright covers the populated and empty states, spacing, and viewport fit.
- **Tab icon polish (2026-09-25):** the existing four bottom destinations use one centered Lucide icon above each unchanged Russian label. The single moving translucent selection bubble, route logic, Search focus behavior, and accessibility names/current-page semantics remain intact. On 320px, all labels and selection states fit; active icons use the blue accent, and admin Statistics stays in its existing header position. Browser screenshots and viewport tests cover the icon/label geometry; actual Telegram WebView remains unverified.
- **UI redesign (2026-09-24):** интерфейс Mini App получил семантические токены цвета и отступов поверх Telegram theme, спокойную типографику и поверхности, быстрый feedback при нажатии и короткие направленные переходы между маршрутами. Поддержаны `prefers-reduced-motion`, `prefers-reduced-transparency` и `prefers-contrast`; HashRouter, Telegram BackButton, viewport/safe-area integration и доступ к материалам сохранены. На низком portrait-экране и при фокусе поиска главная допускает вертикальную прокрутку, чтобы не скрывать содержимое за клавиатурой. Корпоративная тёмная палитра использует тёмно-синие поверхности, фирменный синий акцент и читаемые уровни текста; режим определяется Telegram `colorScheme`, а `themeChanged` возвращает светлую палитру без потери введённого поиска.
- **Latest UI QA (2026-09-24):** после mobile-native/UI polish прошли lint, typecheck, 18 Vitest и 41 Node tests, build, `git diff --check` и Playwright 69/69 в Chromium, mobile Chromium и WebKit. Browser audit проверил 320/390/428 px, светлую/тёмную тему, короткий viewport и focus поиска, long semester, empty folder, safe areas (6/6) и противоположные темы ОС/Telegram: `theme-color` следует Telegram в трёх движках. Сквозная проверка навигации 3/3 подтвердила существующие маршруты, BackButton, in-app back и возврат после hash reload. Горизонтального overflow и page errors нет. Реальный Telegram WebView/устройство, экранная клавиатура, iOS zoom и тактильный отклик ещё не проверялись.
- **Liquid Glass pass (2026-09-24):** добавлены роли материала `thin`/`regular`/`thick`, лёгкие границы, подсветка края и тень. Полупрозрачность применяется выборочно к шапке, поиску, подсказкам и кнопкам возврата; контентные карточки используют полупрозрачный `--material-content` без отдельного blur. При `prefers-reduced-transparency`, повышенном контрасте или отсутствии `backdrop-filter` используются читаемые непрозрачные поверхности. Закреплённая шапка учитывает верхний Telegram safe-area inset и закрывает текст в безопасной зоне при прокрутке. Browser QA подтвердил это в Chromium, mobile Chromium и WebKit; реальный Telegram WebView ещё не проверен.
- **Liquid Glass polish (2026-09-25):** существующие ссылки верхней навигации получили общий набор thin-material edge/highlight/pressed states без нового blur слоя; это был промежуточный этап до переноса глобальной навигации в bottom bar. Токены и Apple sources находятся в `design-system.md`.
- **Persistent Liquid Glass bottom navigation (2026-09-25):** global Catalog/Search/Profile controls are in a fixed floating bar with one moving selection bubble; the brand remains in the header. The Statistics link is separate from the bar and appears at the right of the header only after the existing admin API confirms access. Existing HashRouter destinations, route hierarchy and `goBack` are preserved. Catalog routes select Catalog; search query/results select Search (results preserve Search provenance through router state). Selecting Catalog opens `/`; Search restores the last query and focuses its field. «Назад» remains as the only contextual action; «К корню курса» was removed at the user's request. Bottom reserve lets short pages scroll the footer fully above the panel; the bar does not minimize on scroll.
- **Course 3 Yandex catalog (2026-09-25):** the home card now browses `BMSTU/IU5/3 course` through the public share root, with API paths correctly prefixed for semesters, folders, favorites and downloads. The folder structure is live. Tagged search is intentionally curated from the existing metadata tables; new Course 3 subject folders need tagging before they appear in Search suggestions.
- **Homepage search placement (2026-09-25):** центральная строка поиска убрана с главной после переноса Search в нижнюю навигацию. Поиск и его suggestions доступны на экране Search; маршруты, запросы и переходы к результатам не менялись.
- **Homepage Useful Links section (2026-09-25):** на главной под курсами добавлен отдельный пустой блок «Полезные ссылки» с коротким пояснением; он не меняет маршруты или каталог.
- **Profile and favorites (2026-09-25):** `/#/profile` показывает имя/username из Telegram UI context и список личных папок/файлов; сердца в каталоге синхронизируются через один Worker GET и optimistic PUT/DELETE. Worker проверяет initData и выводит стабильный псевдоним из отдельного `USER_ID_HMAC_SECRET`; миграция `0002_favorites.sql` добавила постоянную таблицу в существующую D1. Worker и Pages обновлены, production API smoke подтвердил 401 без initData и CORS methods для PUT/DELETE.
- **Favorite title layout (2026-09-25):** folder cards use a consistent vertical stack at every width: icon first, left-aligned title below, then the folder hint. The favorite control stays in the upper-right corner and titles can use the card width; file and Profile rows retain dedicated control columns.
- **Bottom navigation capsule geometry (2026-09-25):** the fixed tab bar is now an individual, compact pill surface with semicircular ends; its single moving selection bubble is inset as a smaller concentric capsule. Existing contextual Back/course-root actions remain separate controls above the bar. No routes, roles, tab order, or navigation handlers changed. Browser QA visually reviewed the root and nested Catalog screenshots; the real Telegram WebView remains an outstanding device check.
- **Bottom navigation browser QA (2026-09-25):** временная расширенная Playwright matrix прошла 15/15 в Chromium, mobile Chromium и WebKit: student/admin 2/3-tab layouts, 320/390/428 widths, light/dark, safe-area inset 24 px, hit targets, deep hash reload, Search provenance/context restore, BackButton, unknown/unauthorized routes, keyboard-like 500 px viewport и scroll-to-footer. Permanent tests add route and context regression plus visual screenshots. Final `npm run qa` passed: lint, typecheck, 21 Vitest, 41 Node tests, build and Playwright 75/75. Footer остаётся контентом страницы и полностью доступен после прокрутки; реальная экранная клавиатура и Telegram WebView остаются устройственно-зависимой проверкой.
- **Mobile-native polish (2026-09-24):** `viewport-fit=cover` дополнен `interactive-widget=resizes-content`; `theme-color` синхронизируется с Telegram `colorScheme` через integration layer. CSS использует `100dvh` как fallback до Telegram viewport px, убирает системную вспышку нажатия, ограничивает hover точным указателем и оставляет запрет выделения только на controls. Маршруты, `HashRouter` и логика BackButton не менялись; см. навигационный инвариант в `telegram.md`.
- **Tag-based folder search:** production search uses only the curated «Теги» and «Преподаватель» values from the 29-folder Excel queue, via compact `src/generated/search-folder-index.json`. Both fields are equal; matching is a Unicode-normalized, case-insensitive prefix lookup from the first character. Suggestions update while typing and open the matched folder directly; suggestions and result cards show the folder's parent path beneath its name to distinguish duplicate names. Display `path` and Yandex public-resource `diskPath` are separate: the latter strips the public root name and keeps the API-required leading slash. Blank fields are not indexed, and text search no longer traverses Yandex Disk. The full 8,338-row inventory and metadata index are unchanged.
- **Latest code review (2026-09-23):** поиск теперь собирает совпадения из всех подключённых курсов, сохраняя общий 10-секундный дедлайн. Worker создаёт запись пользователя при первом событии, ограничивает `/stats` личным чатом администратора, считает 7/30 суток скользящими интервалами и принимает только точные административные маршруты. Опциональные CI deploy jobs требуют публичный HTTPS origin Worker как repository variable `VITE_ANALYTICS_API_BASE` до production сборки; Cloud.ru enable flag также должен быть repository variable. Edge rate limit для analytics ещё не настроен.
- **UI QA:** local Playwright 1.63 critical suite uses a Vite `webServer`, deterministic Telegram WebApp and Yandex Disk fixtures, Chromium/iPhone-like Chromium plus local WebKit. GitHub Actions runs Chromium after the existing lint/typecheck/unit/build gate; browser failure artifacts are retained. The browser fixture is test-only and does not alter Mini App security or production data.
- **Completed:** React/Vite SPA, routing, статический `MaterialsRepository`, responsive UI, CI, unit tests и локальный browser smoke test. `npm run dev:telegram` создаёт development-only Quick Tunnel к loopback Vite с HTTP/2; это подготовленный local test path, а не подтверждённый Telegram WebView test или production deployment.
- **In progress:** замена остальных demo-ссылок реальными каталогами Яндекс.Диска. Первый курс уже подключён к публичной папке; третий курс сохранён в интерфейсе без URL, пока материалы готовятся.
- **Next:** добавить публичные каталоги для 3-го и остальных курсов, когда они будут готовы; после следующего запуска Mini App проверить первую строку агрегатов на admin dashboard.
- **Known blockers:** публичные каталоги для 3-го и остальных неподключённых курсов. Production D1 `iu5hub-analytics` создана and migration `0001_analytics.sql` applied on 2026-09-21; Worker deployed with server-only secrets, a protected Telegram webhook and a production-only public API origin for the Pages build. Ни один secret не должен попадать в Git или frontend bundle.
- **Important architecture facts:** один статически собираемый React/TypeScript/Vite frontend существует только внутри Telegram Mini App; UI зависит от `MaterialsRepository` и централизованного Telegram integration layer; Cloudflare Pages — technical host; тяжёлые файлы остаются на Яндекс.Диске. Private analytics is a narrow Worker/D1 exception: it validates `initData`, derives a server-keyed pseudonym, stores minimal events and supplies protected `/stats`; see `analytics.md` and `security.md`.
- **Team workflow:** постоянная небольшая команда; новые роли создаются только для действительно новой специализации, а обычные задачи направляются существующим агентам.
- **Last significant change:** 2026-09-21 — опубликован Worker `iu5hub-analytics` и подключена D1 production database; сервер валидирует Telegram `initData`, сохраняет только HMAC-псевдоним и минимальные события, а администратор получает агрегаты в Mini App dashboard или командой `/stats`. У Pages production build задан публичный `VITE_ANALYTICS_API_BASE`, поэтому следующий автоматический deploy направляет события в Worker. Главная страница оставляет только поиск и каталог курсов; поиск рекурсивно находит реальные папки и файлы в подключённых публичных каталогах Яндекс.Диска, в том числе вложенные файлы УТП.
- **Current UI guard:** на mobile длинный заголовок плитки семестра использует полную ширину после отдельной строки значка. Даже `Парадигмы и конструкции языков программирования` остаётся внутри границ и переносится только по пробелам; файл в той же папке занимает всю строку с фиксированной кнопкой скачивания. Playwright измеряет `scrollWidth` заголовков и геометрию карточки на 320 и 390 px. После редизайна отдельно проверяется узкий экран 320 px, тема и accessibility preferences.
- **Previous UI QA milestones:** 2026-09-21 — локальная гарнитура ALS Sector была подключена для всего интерфейса. После редизайна 2026-09-24 основной текст использует системный стек, а бренд — ALS Sector; Playwright загружает Regular и Bold и проверяет шрифт бренда и корневого элемента. Все папки семестров в `sem`/`семестр`, независимо от регистра, используют единую сетку из двух плиток на mobile. Заголовки папок переносятся только по пробелам: общее правило больше не разрывает слово в середине. Playwright проверяет русское имя папки семестра, колонки и computed word wrapping. Единый публичный asset `/logo-iu5.jpeg` заменён на предоставленный знак «5», поэтому новая иконка отображается в шапке всех маршрутов; Playwright проверяет его успешную загрузку. На главной обычный portrait занимает фактический Telegram viewport без лишней прокрутки, а низкий portrait, фокус поиска и landscape допускают вертикальную прокрутку, когда контент не помещается; горизонтальная прокрутка заблокирована. Playwright проверяет эти режимы. У всех карточек файлов фиксированная правая колонка скачивания: короткое и длинное название не могут сдвинуть кнопку, а Playwright измеряет симметрию на desktop и mobile во всех browser engines. Ранее устранены критическая ARIA-ошибка search suggestions, отсутствие `h1` на главной, race при быстром переходе между папками, stale download error после перехода в другой каталог и clipping подсказки поиска под каталогом на iPhone. Шапка теперь имеет свой слой: hero больше не перехватывает касание ссылки «Статистика» администратора на главной. Playwright проверяет переход на защищённый dashboard в Chromium, mobile Chromium и WebKit. Поиск по живому Яндекс.Диску имеет общий 10-секундный дедлайн, поэтому зависший обход показывает ошибку вместо вечного loader и сначала углубляется в совпавшую папку, чтобы вернуть подходящий вложенный файл (включая УТП). Подсказки также сопоставляют латинскую транслитерацию с кириллическими именами: `Ma` находит `Математический анализ`. Все три курса подключены к публичным папкам; для третьего share root `BMSTU` сочетается с вложенным путём `IU5/3 course`.
- **Deployment policy:** `deploy-cloudru` и `deploy-cloudflare-pages` запускаются только после `verify`, только для push в `main` и только при соответствующем literal enable variable; оба публикуют только статический Mini App. Cloud.ru job генерирует `error.html`, cache-ит hashed `assets/` как immutable, sync-ит остальные root files с no-cache и публикует `index.html` последним с no-store. Технический host URL не распространяется студентам; Bot/Mini App URL остаётся пользовательским входом.
- **Local Telegram development:** для временного ручного теста использовать `npm run dev:telegram` и `@iu5_archive_bot` только как designated dev-test bot. URL Quick Tunnel меняется с каждым запуском, не хранится в Git и не подтверждает настройку бота, пока реальный запуск не проверен.
